# Budget verification

Generated by `pnpm verify:budgets` (2026-09-25) against https://propwash-sim.vercel.app, Chrome (chrome) on AMD Ryzen 9 7900X 12-Core Processor, NVIDIA GeForce RTX 4070 SUPER.

| Check | Result | Measurement |
| --- | --- | --- |
| Initial download ≤ 8 MB | pass | 4.15 MB transferred before the first frame (Other 3.81, Script 0.33, Document 0.00, Fetch 0.00) |
| First frame ≤ 3 s on 50 Mbps | pass | 2.22 s from navigation to the first rendered frame (20 ms latency) |
| No console errors on load | pass | none |
| Frame time, orbit view, 2560×1440 High (webgpu) | pass | orbit: median 2.2 ms (455 fps), p95 3.2 ms, p99 4.3 ms |
| Frame time, fpv view, 2560×1440 High (webgpu) | pass | fpv: median 1.9 ms (526 fps), p95 2.9 ms, p99 3.6 ms |
| Frame time, hd view, 2560×1440 High (webgpu) | pass | hd: median 1.9 ms (526 fps), p95 3.0 ms, p99 3.6 ms |
| No console errors while running | pass | none |
| FPV view shakes with the frame (visible, never cartoonish) | pass | camera travel over 90 frames at 22584 RPM: x 0.079, y 0.111, z 0.058 mm peak-to-peak (includes the ±0.05° body rotation at the lens offset) |

Frame times are measured with vsync and the frame-rate limit off, so they show headroom rather than the display's refresh. This machine is faster than the PRD's RTX 3060 / M1 budget target, so passing here isn't proof for that class of GPU; the dynamic resolution scale (`src/config/render.ts`) is what holds 60 fps on slower hardware.
