# public/audio

Generated by `pnpm assets` from `assets-src/audio/drone.mp3` when that file exists locally. The recording's license is unverified, so the files are git-ignored and the deployed build uses procedural audio only. See `assets-src/audio/LICENSE-NOTE.md`.
