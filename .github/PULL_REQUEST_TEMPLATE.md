## What

<!-- What does this change and why? Link the issue if there is one. -->

## How it was verified

- [ ] `pnpm lint && pnpm typecheck && pnpm test`
- [ ] `pnpm assets && pnpm verify-assets` (if the asset pipeline changed)
- [ ] Screenshots attached (required for rendering, animation, or UI changes)
- [ ] Tunable values live in `src/config/*.ts`, not inline
